<div class="container mt-4">
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="mb-0">Messages</h3>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table" style="table-layout: fixed;">
                    <thead>
                        <tr>
                            <th scope="col">Status</th>
                            <th scope="col">Historic count</th>
                            <th scope="col">Current count</th>
                            <th scope="col">Total count</th>
                            <th scope="col">Throughput</th>
                            <th scope="col">Latency</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% ['queued', 'dequeued', 'publishing', 'published', 'failed'].each do |status| %>
                            <tr>
                                <td class="text-capitalize">
                                    <a class="custom-link" href="<%= outboxer_path("/messages?status=#{status}") %>">
                                        <%= status %>
                                    </a>
                                </td>
                                <td><%= messages_metrics[status.to_sym][:count][:historic] || '-' %></td>
                                <td><%= messages_metrics[status.to_sym][:count][:current] %></td>
                                <td><%= messages_metrics[status.to_sym][:count][:total] || '-' %></td>
                                <td><%= messages_metrics[status.to_sym][:throughput] %></td>
                                <td><%= messages_metrics[status.to_sym][:latency] %></td>
                            </tr>
                        <% end %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="container mt-4">
    <div class="card mt-4">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="mb-0">Publishers</h3>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table" style="table-layout: fixed;">
                    <thead>
                        <tr>
                            <th scope="col">Name</th>
                            <th scope="col">Started</th>
                            <th scope="col">Updated</th>
                            <th scope="col">Status</th>
                            <th scope="col">Throughput (/s)</th>
                            <th scope="col">CPU Usage (%)</th>
                            <th scope="col">Memory Usage (RSS)</th>
                            <th scope="col">RTT (ms)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% publishers.each do |publisher| %>
                            <tr>
                                <td><%= publisher.name %></td>
                                <td><%= time_ago_in_words(publisher.created_at) %></td>
                                <td><%= time_ago_in_words(publisher.updated_at) %></td>
                                <td class="text-capitalize"><%= publisher.info['status'] %></td>
                                <td><%= publisher.info['throughput'] || 0 %></td>
                                <td><%= publisher.info['cpu_usage'] %></td>
                                <td><%= human_readable_size(publisher.info['rss']) %></td>
                                <td><%= (publisher.info['rtt'] * 1000).round(2) %> ms</td>
                            </tr>
                        <% end %>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer"></div>
    </div>
</div>
